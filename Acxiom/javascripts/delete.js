function deleteReminder() {
    // Get the values of the input fields
    const date = document.getElementById("date").value;
    const time = document.getElementById("time").value;
    
    // Delete the reminder from the database or storage
    // This code is just an example and will not actually delete the reminder
    
    // Show a message to the user that the reminder has been deleted
    alert("Reminder has been deleted successfully");
  }
  