const form = document.querySelector('form');
const reminderId = document.querySelector('#reminder-id');
const statusMessage = document.querySelector('#status-message');

form.addEventListener('submit', (e) => {
  e.preventDefault();
  
  // TODO: Send request to backend to enable reminder with given ID
  // If successful, display success message in status message
  // If unsuccessful, display error message in status message
});
