function modifyReminder() {
    // Get the values of the input fields
    const date = document.getElementById("date").value;
    const time = document.getElementById("time").value;
    const description = document.getElementById("description").value;
    
    // Modify the reminder in the database or storage
    // This code is just an example and will not actually modify the reminder
    
    // Show a message to the user that the reminder has been modified
    alert("Reminder has been modified successfully!");
  }
  